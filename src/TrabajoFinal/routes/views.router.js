import { Router } from "express";
const router = Router();

router.get('/', (req, res) => {
  /*   aca renderiza la vista que le pase */
    res.render('home')
});
router.get('/', (req, res) => {
  res.render
 });

export default router;